const gameBoard = (() => {
  //Module pattern -> use only once
  const board = [];
  return { board };
})();

const displayController = () => {
  //Module pattern -> use only once
};

const player = () => {
  //factory function pattern -> to use again
};
